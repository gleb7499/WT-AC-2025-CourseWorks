# Инструкции для написания курсового проекта "Веб-Технологии" (100 баллов)

Ты работаешь ЛОКАЛЬНО на машине пользователя.
Ты НЕ используешь GitHub для коммитов до этапа CI/CD.
Ты вносишь изменения напрямую в файловую систему проекта.

ТВОЯ ЦЕЛЬ:
Создать production-ready full-stack приложение по методичке курса «Веб-Технологии»
(клиент + сервер + БД + документация + тесты + деплой + CI/CD + наблюдаемость),
соответствующее ВСЕМ требованиям на 100 баллов (MVP + все бонусы).

ОСНОВНЫЕ ИСТОЧНИКИ ИСТИНЫ (в порядке приоритета):

1. Файл с вариантом, который дан прямо в ЭТИХ инструкциях (ниже - [PROJECT VARIANT — SOURCE OF TRUTH]).
2. Документация в папке `task_01/`:
   - R1_Routes.md
   - R1_Roles_and_Actions.md
   - R1_Matrix_of_Rights.md
   - R1_DataModel_and_API.md
   - R1_ERD.md
   - R1_WBS.md
   - R1_Cards.md
3. Текущие промты пользователя.

ВАЖНО:

- Документация в `task_01/` может содержать неточности.
- Если ты обнаруживаешь логические противоречия (в ролях, маршрутах, модели данных,
  сценариях, матрице прав) — ОБЯЗАТЕЛЬНО:
  1) явно указать, в чём проблема;
  2) предложить корректировку;
  3) дождаться подтверждения пользователя перед продолжением.

СТЕК (ФИКСИРОВАННЫЙ ДЛЯ MVP + БОНУСЫ):

MVP:

- Frontend: React + TypeScript + Vite
- Backend: Node.js + Express + TypeScript
- БД: PostgreSQL
- ORM: Prisma
- Auth: JWT (access + refresh tokens)
- Validation: Zod

Бонусы:

- Документация API: OpenAPI 3.0 + Swagger UI
- Тестирование: Vitest (unit/integration) + Playwright (e2e)
- Контейнеризация: Docker + Docker Compose
- Оркестрация: Kubernetes (манифесты: Deployment, Service, Ingress, ConfigMap, Secret, PVC)
- CI/CD: GitHub Actions
- Наблюдаемость: структурированные логи (pino), метрики Prometheus, кэш Redis

РЕКОМЕНДУЕМАЯ СТРУКТУРА ПРОЕКТА (обязательно соблюдать):

```
apps/
  server/   # backend
  web/      # frontend
packages/   # общие пакеты (ui, utils)
docs/       # документация (architecture.md, api.md)
infra/      # скрипты/инфраструктура
k8s/        # Kubernetes
docker-compose.yml
pnpm-workspace.yaml
package.json
```

ENV / ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ:

- НЕ использовать корневой `.env` и `.env.example`.
- Использовать два файла окружения:
  - `apps/server/.env` — сервер (DATABASE_URL, JWT_* и т.д.)
  - `apps/web/.env` — клиент (VITE_API_URL и т.д.)
- Список переменных и примеры значений описывать в README соответствующего приложения.

WORKSPACE/ПАКЕТНЫЙ МЕНЕДЖЕР:

- Использовать pnpm (workspace) как основной.
- Скрипты и команды в промтах писать под pnpm (с fallback на npm только если явно нужно).

БАЛЛЫ:

MVP (обязательный минимум): 50 баллов

- Full-stack: SPA клиент + REST API сервер + PostgreSQL
- JWT-аутентификация (register/login) с access + refresh tokens
- Роли: user, admin (минимум)
- Валидация данных на клиенте и сервере (Zod)
- Человекочитаемые ошибки
- CORS, helmet, базовое логирование
- Безопасное хранение паролей (bcrypt)

Бонусы (опционально, суммарно +50 баллов):

- Документация API (OpenAPI/Swagger): +8 баллов
- Тестирование (unit/integration + e2e): +15 баллов
- Деплой в Kubernetes: +15 баллов
- CI/CD (GitHub Actions): +7 баллов
- Наблюдаемость/оптимизация (логи, метрики, кэш): +5 баллов

ИТОГО: 100 баллов

ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ (MVP):

- JWT-аутентификация (register/login) с соблюдением практик безопасности
- Access + Refresh tokens с ротацией
- Роли минимум: user, admin
- Валидация данных на сервере и клиенте
- Человекочитаемые ошибки
- CORS и helmet для обеспечения базовой безопасности
- Безопасное хранение паролей (bcrypt)
- Базовое логирование
- PostgreSQL + Prisma migrations
- Конфигурация через `apps/server/.env` и `apps/web/.env` (без корневых env-файлов)
- Архитектурная грамотность: разделение слоёв (routes, controllers, services, models)
- Возможность запустить проект локально по инструкции из README

ТРЕБОВАНИЯ К БОНУСАМ:

Документация API (+8):

- OpenAPI 3.0 спецификация
- Swagger UI доступен по /api-docs
- Все эндпоинты задокументированы (включая auth)
- Примеры запросов и ответов
- Описание схем данных

Тестирование (+15):

- Unit-тесты для сервисов и утилит (Vitest)
- Integration-тесты для API эндпоинтов (Vitest + supertest)
- E2E-тесты для критических пользовательских сценариев (Playwright)
- Coverage отчёты
- Тесты запускаются одной командой

Kubernetes (+15):

- Dockerfile для backend и frontend
- Docker Compose для локальной разработки
- K8s манифесты:
  - Deployment (backend, frontend, postgres, redis)
  - Service (ClusterIP, LoadBalancer/NodePort)
  - Ingress (с TLS annotations)
  - ConfigMap (non-sensitive config)
  - Secret (credentials)
  - PersistentVolumeClaim (postgres data)
- Probes: readiness, liveness
- Resource limits/requests
- HPA (Horizontal Pod Autoscaler) — опционально

CI/CD (+7):

- GitHub Actions workflow
- Stages: lint → test → build → push images
- Кэширование зависимостей
- Secrets management
- Multi-stage Docker builds

Наблюдаемость (+5):

- Структурированные JSON логи (pino)
- Prometheus метрики (/metrics endpoint)
- Redis кэширование (сессии и/или часто запрашиваемые данные)
- Health checks (/health, /ready)

ЗАПРЕЩЕНО:

- Делать «предположения» о бизнес-логике, если она не следует из варианта или R1.
- Добавлять технологии, не указанные в стеке.
- Менять стек без явного указания пользователя.
- Жертвовать безопасностью ради скорости реализации.
- Игнорировать базовые принципы архитектурной грамотности.
- Хардкодить секреты в коде или манифестах.
- Создавать избыточную сложность без необходимости.

ПРАВИЛО РАБОТЫ:
Каждый этап = минимально достаточная реализация + проверяемый результат.
Все решения должны учитывать безопасность и архитектурную грамотность с самого начала.
Если этап не проверяем локально — он считается невыполненным.
Бонусные компоненты должны интегрироваться с MVP без нарушения его работоспособности.

ПОРЯДОК ЭТАПОВ:

Фаза 1 — MVP (этапы 0-7): базовое приложение на 50 баллов
Фаза 2 — Бонусы (этапы 8-13): расширения на +50 баллов
Фаза 3 — Финальный аудит (этап 14): проверка на 100 баллов

[PROJECT VARIANT — SOURCE OF TRUTH]

```
## Вариант 40 — Заявки «Да, я в деле» 📝
Питч: заявки проходят, события случаются.
- MVP: формы заявок, статусы, роли, файлы.
- API: /applications, /forms, /statuses, /attachments.
- Данные: Application, Form, Status, Attachment, User.
- Приёмка (MVP): трекинг статуса.
- Бонусы: согласование по этапам; Документация API, Тесты, Kubernetes, CI.
```

Этот текст имеет приоритет над любыми догадками агента.
