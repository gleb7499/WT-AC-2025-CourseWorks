openapi: 3.0.3
info:
  title: Variant 40 API
  version: 0.0.0
  description: Placeholder OpenAPI spec for future endpoints.
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
